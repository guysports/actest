# Default value for kube-audit-logger
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicas: 1

constraints:
  reqmem: "100Mi"
  reqcpu: "0.2"
  limmem: "500Mi"
  limcpu: "0.5"

global:
  ef:
    registry: us.icr.io/mhub
    images:
      tag: 1
